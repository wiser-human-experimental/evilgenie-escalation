# Copy to .env and fill in. .env is git-ignored.

# Required — every agent and judge model routes through OpenRouter.
OPENROUTER_API_KEY=sk-or-v1-...

# Optional — only needed for the two-stage disclosure/monitoring classifiers
# in verification/ (they use the OpenAI Batch API directly).
# OPENAI_API_KEY=sk-...
